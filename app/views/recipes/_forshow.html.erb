<center> 
  <h2><%= @recipe.name %></h2>
</center>
  <br>
  <p><strong>Category:</strong> <%= @recipe.category.try(:name) %></p>
  <p><strong>Vegetarian:</strong> <%= @recipe.vegetarian %></p>
  <p><strong>Ingredients:</strong> <% @recipe.ingredients.each do |ingredient| %>
                  <%= simple_format ingredient.name %>
                  <% end %></p>
  <br>
  <center>
    <strong>Instructions:</strong>
  </center>
  <%= @recipe.instructions %>
  <br>
  <br>
  <center>
    <%= link_to("Edit Recipe", edit_recipe_path(@recipe), class: "bueton") %> <%= button_to('Delete Recipe', recipe_path(@recipe), method: :delete, class: 'bueton', data:{confirm: 'Confirm Deleting'}) %>
  </center>