  <h3>
    <%= link_to("#{category.name}", category_path(category), class: "categoryname") %>
  </h3>
  <%= link_to("Edit Category", edit_category_path(category), class: "bueton") %> <%= button_to('Delete Category', category_path(category), method: :delete, class: 'bueton', data:{confirm: 'Confirm Deleting'}) %>
<br>